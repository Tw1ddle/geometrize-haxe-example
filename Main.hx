package;

import geometrize.bitmap.Bitmap;
import geometrize.runner.ImageRunner;
import geometrize.bitmap.Rgba;

/**
 * Minimal native-only demo using Geometrize Haxe to recreate images with geometric primitives.
 * @author Sam Twidale (https://www.geometrize.co.uk/)
 */
class Main
{
	private static function main():Void {
		// TODO need to actually implement this
		var bitmap = Bitmap.create(0, 0, Rgba.create(255, 128, 255, 255));
		//var runner = new geometrize.runner.ImageRunner();
	}
}